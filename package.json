{
  "name": "nocturne",
  "version": "0.0.1",
  "description": "Turn LLM replies into beautiful, shareable newspaper-style web pages. A NyxID downstream service.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "start": "bun run src/server.ts",
    "build": "mkdir -p dist && bun build --compile --minify --target=bun-linux-x64 ./src/server.ts --outfile ./dist/nocturne",
    "typecheck": "bunx tsc --noEmit",
    "test": "bun test src/schema src/utils src/storage src/index src/middleware src/llm src/renderer src/routes/view.test.ts src/routes/rate.test.ts src/routes/archive.test.ts src/routes/archive-share.test.ts test/e2e test/eval && bun test src/routes/format.test.ts",
    "test:coverage": "bun test --coverage"
  },
  "dependencies": {
    "@hono/zod-openapi": "^0.19.0",
    "hono": "^4.6.0",
    "jose": "^5.9.0",
    "pino": "^9.5.0",
    "postgres": "^3.4.5",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
